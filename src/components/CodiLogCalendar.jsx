import style from '../css/Codi.module.css'

const CodiLogCalendar = ({ feltWeather }) => {
    console.log('필터 종류 전달됨 - 달력', feltWeather);

    return (
        <section >CodiLogCalendar</section>
    )
}

export default CodiLogCalendar